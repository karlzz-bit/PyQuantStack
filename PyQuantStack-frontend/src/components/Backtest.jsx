import React from 'react';

const Backtest = () => {
  return (
    <div>
      <h1>量化策略回测</h1>
      <p>配置策略参数、执行回测，并展示回测曲线、绩效指标等。</p>
      {/* 可集成图表和参数输入组件 */}
    </div>
  );
};

export default Backtest;
